[project]
name = "verifiled"
version = "0.5.0"
description = "A small package for transferring files and directories across potentially flaky filesystems."
readme = "README.md"
requires-python = "==3.13.*"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
